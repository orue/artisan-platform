# Artisan Platform — GitHub Actions: terraform-plan
# Implemented in Step 13 (CI) / Step 16 (CD)
name: terraform-plan
"on":
  workflow_dispatch: {}
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Placeholder — implemented in a later step"
